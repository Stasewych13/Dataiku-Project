# MEMORY.md — жива пам'ять про роботу з Dataiku

> **Призначення.** Цей файл фіксує **фактичний** стан і процес роботи в нашому Dataiku — на противагу `docs/`, які описують платформу «як має бути» за офіційною документацією. Тут — «як є насправді»: конкретний інстанс, версія, connections, conventions, відомі особливості й відхилення від загальних знань.
>
> **Хто і коли пише.** Claude (через skill `dataiku`) звіряється з цим файлом **перед** кожною задачею і **дописує** його, коли:
> - фактичний стан у програмі **відрізняється** від `docs/` (відхилення, особливість версії, кастомний плагін, нестандартна поведінка);
> - з'явився **новий стійкий факт** про інстанс/процес (нова connection, convention, прийняте рішення);
> - закрито або відкрито **open question**.
>
> **Формат запису.** Кожен факт: `YYYY-MM-DD` + суть + джерело/контекст. Лаконічно. Не дублюй `docs/` — сюди лише специфіку нашого інстансу. Сумнівне познач `(?)` і винеси в Open questions.

**Статус наповнення:** 🟡 шаблон — більшість фактів про інстанс ще не внесено. Заповнюється в міру роботи з робочого комп'ютера ПриватБанку.

> 🔒 **БЕЗПЕКА / КОМПЛАЄНС (читати перед записом!).** Dataiku розгорнуто self-managed на **внутрішньому сервері ПриватБанку**. Якщо репозиторій публічний — **НЕ ЗАПИСУВАТИ** сюди: внутрішні URL/хости, реальні рядки connections (хости БД, schema), credentials/ключі, повні імена проєктів і чутливу структуру пайплайнів. Це витік внутрішньої банківської інфраструктури. Зробити репо приватним: `gh repo edit Stasewych13/Dataiku-Project --visibility private`. У публічному варіанті — лише знеособлені факти (тип ноди, версія, узагальнені conventions), без ідентифікаторів.

Останнє оновлення: 2026-06-24 — підтверджено self-managed on-prem; додано гайд «де подивитися».

---

## 1. Інстанс і доступ

> Заповнити при першому контакті з живим DSS.

- **Тип:** ✅ **self-managed on-prem** — розгорнуто на внутрішньому сервері ПриватБанку (підтверджено 2026-06-24). Отже `docs/` про Dataiku Cloud/Fleet Manager — довідково, не наш кейс.
- **DSS версія:** `TBD` → подивитися: іконка **`?`** (Help Center) у верхній панелі → прокрутити **вниз** → **About**. ⚠️ НЕ під аватаром (перевірено 2026-06-24: у меню аватара кнопки About немає). Резерв: версія у шляху посилання Documentation (`doc.dataiku.com/dss/<версія>/`), внизу сторінки логіну, або `dataiku.api_client().get_instance_info().raw.get("dssVersion")`. Важливо: визначає точні UI-підписи; `docs/` орієнтовані на 13.x/14.x.
- **Edition / ліцензія:** `TBD` → `Administration → Licensing` (адмін-права). Впливає на UIF, containerized execution, Govern, Advanced LLM Mesh.
- **URL Design node:** не записувати в публічний репо (внутрішній хост банку). Тип ноди визначити за UI: бачиш Flow + Lab + recipes → Design node.
- **Як автентифікуємось:** _(SSO / local / LDAP)_ — `TBD` (для банку найімовірніше корпоративний SSO)
- **Програмний доступ:** є API node? `TBD`; є API key для `dataikuapi`? `TBD` (ключ/хост — НЕ сюди, лише факт наявності)

> **Як визначити параметри інстансу (без адмінки):** версія — іконка `?` (Help Center) → внизу **About** (НЕ аватар!); «це Design node» — якщо є Flow/Lab/recipes; доступні connections — `+ New dataset`. Топологію (Automation/API/Govern), повний список connections і ліцензію найточніше уточнити в **внутрішньої команди адмінів Dataiku ПриватБанку**.

## 2. Топологія нод (deployment)

> Які ноди реально є і як між ними рухаються проєкти/моделі. Деталі концепцій — `docs/01`, `docs/10`.

- **Design node:** `TBD`
- **Automation node:** є? `TBD`
- **API node / API Deployer:** є? `TBD`
- **Project Deployer / Govern node:** є? `TBD`
- **Процес dev→prod:** _(через bundles? вручну? CI/CD?)_ — `TBD`

## 3. Connections (реально використовувані)

> Не credentials — лише назви, типи, призначення та особливості. Концепції — `docs/03`.

| Connection name | Тип | Призначення | Global/Per-user | Нотатки |
|-----------------|-----|-------------|-----------------|---------|
| `TBD` | | | | |

## 4. Conventions проєкту

> Узгоджені правила. Якщо їх ще немає — рекомендований дефолт із `docs/15` нижче, замінити фактичними коли з'являться.

- **Project keys:** `TBD`
- **Naming датасетів:** рекомендовано `foo_raw` → `foo_clean` → `foo_prepared` (`docs/15`); фактично — `TBD`
- **Flow zones:** рекомендовано ділити за стадіями (ingest / prep / ML / output); фактично — `TBD`
- **Tags:** `TBD`
- **Цільові connections для output:** `TBD`
- **Code environments:** які є, на що призначені — `TBD`

## 5. Регулярні робочі процеси / scenarios

> Які пайплайни/scenarios існують, що роблять, розклад, тригери, нотифікації. Концепції — `docs/09`.

| Scenario / pipeline | Що робить | Trigger / розклад | Reporter | Нотатки |
|---------------------|-----------|-------------------|----------|---------|
| `TBD` | | | | |

## 6. ⚠️ Відомі особливості та відхилення від `docs/`

> **Найважливіший розділ.** Сюди — кожен випадок, коли реальність інстансу не збігається із загальними знаннями. Це те, заради чого існує MEMORY.md.

> _(Записів поки немає. Приклад формату — нижче, видали його, коли з'являться реальні.)_
>
> - `2026-06-24` (приклад) — У нашому DSS Visual recipe «Window» недоступний з engine=SQL на connection X через _(причина)_; обхід — _(рішення)_. Розбіжність із `docs/04`, де SQL-engine описаний як доступний. Джерело: спостереження в задачі Y.

## 7. Журнал рішень (decisions log)

> Важливі рішення щодо архітектури/процесу та **чому** саме так. Щоб не передумувати по колу.

> _(Порожньо. Формат: `YYYY-MM-DD` — рішення — причина — альтернативи, що відкинули.)_

## 8. Open questions

> Те, що треба з'ясувати у користувача або перевірити на інстансі. Закриваючи питання — переноси факт у відповідний розділ вище.

- [x] Тип DSS — ✅ self-managed on-prem (сервер ПриватБанку), 2026-06-24.
- [ ] Версія DSS? (аватар → About) — визначає точність `docs/`
- [ ] Які ноди є в топології (Automation / API / Govern)?
- [ ] Які основні connections і формати даних?
- [ ] Чи є усталені naming/zone/tag conventions, чи приймаємо рекомендовані?
- [ ] Який цільовий use-case першого завдання?

---

## Changelog цього файлу

- `2026-06-24` — Створено структуру MEMORY.md (шаблон). База знань `docs/` 01–16 готова. Skill `dataiku` створено.
- `2026-06-24` — Підтверджено: розгортання self-managed on-prem (сервер ПриватБанку). Додано безпекове застереження (репо публічний — не писати внутрішні дані) і гайд «де подивитися параметри інстансу». Працюємо з робочого комп'ютера ПриватБанку.
